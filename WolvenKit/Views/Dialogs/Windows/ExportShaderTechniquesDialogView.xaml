<Window
    x:Class="WolvenKit.Views.Dialogs.Windows.ExportShaderTechniquesDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:sf="http://schemas.syncfusion.com/wpf"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:templates="clr-namespace:WolvenKit.Views.Templates"
    xmlns:helpers="clr-namespace:WolvenKit.Helpers"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:local="clr-namespace:WolvenKit.App.ViewModels.Tools.ShaderCache"
    Title="Export Shader Techniques"
    MinWidth="{DynamicResource WolvenKitDialogWidthSmall}"
    MinHeight="{DynamicResource WolvenKitDialogHeightSmall}"
    Padding="{DynamicResource WolvenKitMarginTop}"
    Background="{StaticResource BackgroundColor_Control}"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterScreen"
    DataContext="{Binding RelativeSource={RelativeSource Self}, Path=ViewModel}"
    >
    <Grid
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch">
        <Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <hc:ThemeResources />
                    <hc:Theme />
                </ResourceDictionary.MergedDictionaries>
                <Style
                    BasedOn="{StaticResource WolvenkitLabelStyle}"
                    TargetType="{x:Type Label}">
                    <Setter Property="HorizontalAlignment" Value="Left" />
                    <Setter Property="FontSize" Value="{DynamicResource WolvenKitFontSubTitle}" />
                </Style>
            </ResourceDictionary>
        </Grid.Resources>

        <sf:WizardControl
            CancelButtonCancelsWindow="True"
            FinishButtonClosesWindow="True"
            >
            <sf:WizardPage
                Height="Auto"
                Background="{StaticResource BackgroundColor_Control}"
                BackVisibility="Collapsed"
                CancelVisibility="Visible"
                FinishVisibility="Visible"
                HelpVisibility="Collapsed"
                NextVisibility="Collapsed"
                FinishEnabled="{Binding CanExport}"
                PageType="Exterior">
                <Grid
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="{StaticResource WolvenKitDialogLabelColumnWidthMedium}" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="{DynamicResource WolvenKitButtonGridWidth}" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Label
                        Grid.Row="0"
                        Grid.Column="0"
                        Content="Export Format" />

                    <ComboBox
                        Grid.Row="0"
                        Grid.Column="1"
                        Grid.ColumnSpan="2"
                        ItemsSource="{Binding ExportFormats}"
                        DisplayMemberPath="Desc"
                        SelectedValuePath="Value"
                        SelectedItem="{Binding Format, Mode=OneWay}"
                        />


                    <Label
                        Grid.Row="2"
                        Grid.Column="0"
                        Content="Shader Types" />

                    <hc:ComboBox
                        Grid.Row="2"
                        Grid.Column="1"
                        Grid.ColumnSpan="2"
                        ItemsSource="{Binding ShaderTypes}"
                        DisplayMemberPath="Desc"
                        SelectedValuePath="Value"
                        SelectedItem="{Binding Type, Mode=OneWay}"
                        >
                    </hc:ComboBox>


                    <Label
                        Grid.Row="4"
                        Grid.Column="0"
                        Content="Output Folder" />

                    <TextBox
                        Grid.Row="4"
                        Grid.Column="1"
                        Margin="{StaticResource WolvenKitMarginTinyRight}"
                        Text="{Binding Folder}"
                        />

                    <Button
                        Grid.Row="4"
                        Grid.Column="2"
                        Padding="0"
                        KeyboardNavigation.TabIndex="-1"
                        Click="FileButton_Click">
                        <templates:IconBox
                            IconPack="Material"
                            Kind="Folder"
                            Size="{DynamicResource WolvenKitIconMicro}" />
                    </Button>

                </Grid>
            </sf:WizardPage>
            
        </sf:WizardControl>
        
    </Grid>
</Window>
